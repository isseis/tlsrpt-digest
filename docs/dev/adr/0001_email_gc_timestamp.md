# ADR-0001: `.eml` ファイルの GC 判定に使用する日時の選択

| 項目 | 内容 |
|---|---|
| 番号 | ADR-0001 |
| ステータス | 採択 |
| 決定日 | 2026-05-20 |
| 関連タスク | 0040_store, 0041_store_gc_simplify |

---

## 1. コンテキスト

### システムにおける `.eml` GC の位置づけ

本システムは IMAP メールボックスから TLSRPT レポートメールを取得し、メール原本を `.eml` ファイルとして `{root_dir}/emails/{uidvalidity}/{YYYYMM}/{padded_uid}.eml` に保存する。`{YYYYMM}` は IMAP サーバーが設定する受信日時（`INTERNALDATE`）から導出し、古いファイルを日付単位で手動削除しやすくするための構造である。

`DeleteEmailsBefore` は保持期間を超えた `.eml` ファイルを定期 GC する。GC の判定基準として、システム内には以下の 4 種類の日時が存在する。

### 利用可能な日時とその特性

| 日時 | 取得元 | 制御主体 | 信頼性 |
|---|---|---|---|
| **送信日時**（SentAt） | メールの `Date:` ヘッダー | 送信側（外部） | 低：外部から任意に設定可能 |
| **IMAP 受信日時**（INTERNALDATE） | IMAP サーバー | メールサーバー（外部） | 中：サーバー依存 |
| **ダウンロード日時**（SavedAt） | ファイルの inode change time（ctime） | 本システム（ローカル） | 高：外部から改ざん不可 |
| **レポート期間終了日**（report_end_date） | TLSRPT レポート内の `date-range.end-datetime` | 送信側（外部） | 低：外部から任意に設定可能 |

---

## 2. 設計上の考慮事項

### 外部制御値を GC 基準にした場合のリスク

`SentAt` と `report_end_date` は送信側が設定する値であり、遠未来日付（例：year 3000）の誤設定や意図的な攻撃によって GC が機能しなくなるリスクがある。`.eml` ファイルが削除されずに蓄積し続けると、ディスク使用量が無制限に増大する。

一方、`SavedAt`（ctime）は本システムが記録するローカル制御の値であり、外部から改ざんできないため GC 基準として信頼性が高い。

### ファイルパス構造と GC 基準日時の関係

`.eml` のパスに含まれる `{YYYYMM}` は `INTERNALDATE`（IMAP サーバー受信日時）由来である。GC 基準の `SavedAt`（ダウンロード日時）とは異なる月になり得るため（例：2025 年 1 月受信・2025 年 6 月ダウンロード）、ディレクトリ名と GC 基準日時を直接比較して削除するアプローチは不整合を生じる。

### `Date:` ヘッダーに依存したパス決定の問題

`{YYYYMM}` の決定に `SentAt`（`Date:` ヘッダー由来）を使用することには以下の問題がある。

- **外部制御値への依存**: `Date:` ヘッダーは送信側が任意に設定できる値であり信頼性が低い
- **フォールバック時のパス不安定性**: `Date:` ヘッダーが欠損している場合は `SavedAt` にフォールバックするが、リカバリー等で同一メールを再取得すると `SavedAt` が変わりパスが変化し、重複ファイルが生じるリスクがある

IMAP サーバーが設定する `INTERNALDATE`（RFC 3501 必須フィールド）は常に存在し、サーバーが一度設定した後は変更されないため、フォールバック不要で安定したパス決定に適している。

### バリデーションの責務配置

遠未来 `report_end_date` への対処として `internal/tlsrpt.Parse()` に `end-datetime` の上限チェックを追加する案も考えられる。しかし `internal/tlsrpt` は RFC 8460 JSON を忠実に Go 構造体へ変換する責務を持つ。「この date-range を処理するかどうか」はアプリケーションレベルの判断であり、パーサーにビジネスロジックを持ち込むと責務が混在し、`now time.Time` を注入する必要も生じてテスト容易性が低下する。この判断はエントリポイントの責務とする。

---

## 3. 検討した選択肢

### 選択肢 A: `report_end_date` と `saved_at` の両基準を維持し、ディレクトリスイープを修正する

GC 対象ディレクトリのスイープをサバイビングインデックスとの照合で誤削除を防ぐ形に修正する。

**却下理由**: 根本原因（送信側制御の値を GC 判定に使うこと）を解消しておらず、`report_end_date` による攻撃ベクターが残存する。コードの複雑性も維持されたままとなる。

### 選択肢 B: `{YYYYMM}` ディレクトリ名を `SentAt` から `SavedAt` に変更する

パスを `{savedAt.YYYYMM}` ベースにすれば、ディレクトリ名と GC 基準の比較が一貫する。

**却下理由**:

- **パスの安定性が `INTERNALDATE` より低い**: `SavedAt` はダウンロード日時であるため、リカバリー等で同一メールを再取得すると月が変わり別パスへ書き込まれ、重複ファイルが生じるリスクがある。`INTERNALDATE`（RFC 3501 必須フィールド）はサーバーが一度設定した後は変更されないため常に安定する。
- **意味的な劣化**: ディレクトリが「レポート期間」ではなく「ダウンロード時期」で分割されるため、特定期間のメールを手動で探しにくくなる。
- **根本問題の未解消**: `SavedAt` はローカル制御値であり GC 基準としては適切だが、パス決定においては `INTERNALDATE` のほうが安定性・信頼性ともに優れる。

### 選択肢 C: `.eml` GC を `saved_at` 一本化し、ディレクトリスイープを正しい実装に置き換える（採択）

- `DeleteEmailsBefore(savedAtCutoff time.Time)` に一本化する（`reportCutoff` パラメータを削除）
- `report_end_date` をメールインデックスから削除する
- ディレクトリ名とGC 基準日時の比較に基づくスイープを廃止し、`DeleteEmailsBefore` 内で GC 後に空になった `{uidvalidity}/{YYYYMM}` および `{uidvalidity}` ディレクトリを削除する正しい実装に置き換える

真に孤立した `.eml` ファイル（インデックスに存在しないファイル）の清掃は `reprocess` サブコマンドが全 `.eml` を再帰走査するため、ディレクトリスイープがなくても自然に回収される。

### 選択肢 D: `internal/tlsrpt.Parse()` に date-range バリデーションを追加する

パース時点で `end-datetime <= now + 48h` などを検証する。

**却下理由**: セクション 2「バリデーションの責務配置」に記載のとおり、パーサーの責務範囲を超えるためエントリポイントで対処する。

---

## 4. 決定

### 4.1 GC 判定日時：選択肢 C を採択

**タスク 0041_store_gc_simplify として実装する**。`.eml` の GC 判定には `SavedAt`（ダウンロード日時）のみを使用する。

### 4.2 パス決定日時：`INTERNALDATE` への一本化

**タスク 0041_store_gc_simplify の前提として同時に実装する**。`{YYYYMM}` の決定を `SentAt`（`Date:` ヘッダー）から `INTERNALDATE` に変更し、`SentAt` をシステムから廃止する。

- `SentAt` を `EmailMeta`・`LoadedEmail`・`internalEmailIndexEntry` から削除する
- `INTERNALDATE` を `InternalDate` フィールドとして `EmailMeta` および `internalEmailIndexEntry` に追加する
- `Store.SaveEmail` の引数を `sentAt` から `internalDate` に変更する
- `INTERNALDATE` がゼロ値の場合は `SavedAt` にフォールバックして `slog.Warn` を出力する（ロバストネス原則に基づく防御的実装）
- 本システムは開発中のため、既存デプロイメントへの後方互換性は考慮しない

| 日時 | `.eml` GC での役割 | パス決定での役割 | レポート GC での役割 |
|---|---|---|---|
| `SentAt` | **廃止** | **廃止** | 使用しない |
| `INTERNALDATE` | 使用しない | **`{YYYYMM}` 決定の基準** | 使用しない |
| `SavedAt` | **削除判定の唯一の基準** | ゼロ値フォールバックのみ | 使用しない |
| `report_end_date` | **廃止**（メールインデックスから削除） | 使用しない | 削除判定の基準（変更なし） |

`report_end_date` は引き続きレポートレコード（`tlsrpt.json` の `reports` 配列）の GC（`DeleteReportsBefore`）に使用する。レポートレコードには `saved_at` 相当の日時がなく、「対象期間が終わったデータを集計対象から外す」という意味論が正しいためである。

遠未来 `end-datetime` への対策はエントリポイント（タスク 0070）での保持期間上限として実装し、`internal/tlsrpt` には追加しない。

---

## 5. 結果として生じるトレードオフ

| 得られるもの | 失うもの |
|---|---|
| GC 判定が外部攻撃に対して堅牢になる | 真に孤立した `.eml` ファイルは `reprocess` 実行まで残る場合がある |
| `DeleteEmailsBefore` のロジックが単純化される | — |
| GC 後の空ディレクトリは `DeleteEmailsBefore` が正しく削除する | — |
| `SaveReports` がメールインデックスを変更しなくなり責務が明確化される | — |
| ディレクトリ名との日時比較を廃止し誤削除リスクがなくなる | — |
| `INTERNALDATE` によりパスが常に安定し、`Date:` ヘッダー欠損時のフォールバック問題が解消される | — |
| `SentAt` を廃止することで外部制御値への依存をパス決定からも排除できる | — |
